version: '3.8'

services:
  vexotts:
    image: ghcr.io/axiom3d-yt/vexotts:latest
    container_name: vexotts
    restart: unless-stopped
    ports:
      - "4200:4200"
    env_file:
      - .env
    # Optional: Volume for persistence if needed in future
    # volumes:
    #   - .:/app
