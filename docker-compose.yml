version: '3.8'

services:
  vexotts:
    build: .
    container_name: vexotts
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Optional: Volume for persistence if needed in future
    # volumes:
    #   - .:/app
